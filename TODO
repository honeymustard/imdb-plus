TODO for project imdb-plus:

 -> CONSIDER THIS:

  * secondary sorting. pros) easy to implement, cons) negligible value
  * add 'List stats' tab. pros) This would take minutes
  * add 'Comparison' tab. compare MyMovies <-> MyLists -> vs IMDB
  * add 'Other stats' tab. lists contain additional statistically significant information..

 -> TO BE FIXED:

  * allow/disallow hidden columns in table widget.. i might need this..
  * make update get MyRatings if they are available..
  * make open dialog default to current/install directory..
  * Header footer text bleeds into other columns..

